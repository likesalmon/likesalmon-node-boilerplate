/**
 *
 *
 * {{ kebabCase moduleName }}
 *
 *
 */
const logger = require('../utils/logger');

const {{ camelCase moduleName }} = () => {
  logger.info("This is {{ camelCase moduleName }}.", { moduleName: "{{ kebabCase moduleName }}" });
};

module.exports = {{ camelCase moduleName }};
