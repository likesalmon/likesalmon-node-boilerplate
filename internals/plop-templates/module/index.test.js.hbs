/**
 * {{moduleName}} tests
 */
require('dotenv').config();

const { main } = require('../index');

describe('{{moduleName}}', () => {
  describe('main', () => {
    it('should return true', async () => {
      expect(main()).toBe(true);
    });
  });
});
