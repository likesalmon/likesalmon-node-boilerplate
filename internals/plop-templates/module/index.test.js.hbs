/**
 *
 *
 * Tests for {{ kebabCase moduleName }}
 *
 *
 */
const {{ camelCase moduleName }} = require('./index');

describe("{{ camelCase moduleName }}", () => {
  it("should not error", () => {
    const spy = jest.spyOn(global.console, "error");
    {{ camelCase moduleName }}();
    expect(spy).not.toHaveBeenCalled();
  });
});